"""Experimental ML layer.

Trains a transparent, walk-forward-validated logistic-regression model on
indicator features to estimate the probability of a positive forward return.

IMPORTANT: outputs are experimental research signals, NOT predictions of the
future and NOT investment advice. Validation accuracy near 50% means the model
has no edge — the API reports metrics honestly so you can see that.
"""
