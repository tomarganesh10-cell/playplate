FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /srv

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app ./app
COPY web ./web

EXPOSE 8000

# Reference deployment. Put a real datastore, auth, and the full Consent &
# Ethics Guard in front before production (see ../../architecture/).
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
